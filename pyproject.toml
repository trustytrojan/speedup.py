[project]
name = "speedup.py"
version = "0.0.1"
authors = [
	{ name="trustytrojan" }
]
description = "A simple script to speedup your music/audio"
requires-python = ">=3.11"

[project.urls]
"GitHub" = "https://github.com/trustytrojan/speedup.py"

[project.scripts]
speedup.py = "speedup:main"
